name: leta_spin_pro
description: Leta Spin Pro - Spin Win Repeat

publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter

flutter:
  uses-material-design: true
  app-name: Leta Spin Pro
